/**
 * Web layer (REST controllers) for Invoice Backend API.
 * Controllers are secured with JWT and RBAC and expose endpoints under /api/*.
 * Incoming webhooks are exposed under /api/webhooks/* and protected via signature headers.
 */
package com.example.invoicebackend.web;
